#include<stdio.h>
int main()
{
	int i,fact=1,number;
	printf("enter a number");
	scanf("%d",&number);
	for(i=i;i<=number;i++)
	{
		fact=fact*1;
	}
	printf("factorial of %d is :%d",number,fact);
	return 0;
}
